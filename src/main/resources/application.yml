spring:
  application:
    name: "auth-template"

  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:postgresql://shiv:fHebXigE5RVWEVE6p5mHS3UgqoODuz7j@dpg-d6602nbnv86c73e0uppg-a.singapore-postgres.render.com/fbmono?sslmode=require}
    username: ${SPRING_R2DBC_USERNAME:shiv}
    password: ${SPRING_R2DBC_PASSWORD:fHebXigE5RVWEVE6p5mHS3UgqoODuz7j}
    pool:
      enabled: true
      initial-size: 10
      max-size: 50

  banner:
    location: banner.txt

  codec:
    max-in-memory-size: 50MB

  webflux:
    base-path: /api/v1
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

  compression:
    enabled: false

  http2:
    enabled: false

security:
  jwt:
    secret: ${JWT_SECRET:ABCDEFGHIJKLMNOQRSTUVWXYZ1234567890abcedfghijklmnoqrstuvwxyz}
    expirationMs: ${JWT_EXPIRATION:3600000}

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.apache.kafka: OFF
    org.quartz: OFF
    org.apache.kafka:clients.NetworkClient: OFF
    org.springframework.scheduling.quartz.LocalDataSourceJobStore: OFF

server:
  port: ${PORT:9090}

