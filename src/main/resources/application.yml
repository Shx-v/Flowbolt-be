spring:
  application:
    name: "auth-template"

  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:postgresql://shiv:8vhdiAUbkE6ghJm692gzJj5dDzmx6c7X@dpg-d94ti6q8qa3s73d7v3v0-a.singapore-postgres.render.com/flowbolt?sslmode=require}
    username: ${SPRING_R2DBC_USERNAME:shiv}
    password: ${SPRING_R2DBC_PASSWORD:8vhdiAUbkE6ghJm692gzJj5dDzmx6c7X}
    pool:
      enabled: true
      initial-size: 10
      max-size: 50

  banner:
    location: banner.txt

  codec:
    max-in-memory-size: 50MB

  webflux:
    base-path: /api/v1
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

  compression:
    enabled: false

  http2:
    enabled: false

security:
  jwt:
    secret: ${JWT_SECRET:ABCDEFGHIJKLMNOQRSTUVWXYZ1234567890abcedfghijklmnoqrstuvwxyz}
    expirationMs: ${JWT_EXPIRATION:3600000}

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.apache.kafka: OFF
    org.quartz: OFF
    org.apache.kafka:clients.NetworkClient: OFF
    org.springframework.scheduling.quartz.LocalDataSourceJobStore: OFF

server:
  port: ${PORT:9090}

